"""AI-CCTV Sentinel — dataset collection & curation tooling (Task 3).

This package holds standalone CLI scripts (inspect_dataset.py,
validate_files.py, calculate_hashes.py, build_manifest.py,
dataset_report.py) plus the shared schemas.py module they all import.
Nothing here performs training, annotation, or model logic — that is
Task 4/5 scope.
"""
